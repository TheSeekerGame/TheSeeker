/*use crate::appstate::AppState;
use crate::prelude::*;

pub struct ProgressBarPlugin;

impl Plugin for ProgressBarPlugin {
    fn build(&self, app: &mut App) {
        app.add_systems(Update);
        app.add_systems(
            OnExit(AppState::InGame),
            despawn_toolbar,
        );
    }
}

pub fn add_bar_material() {}*/
